name: Add a new GitHub Project card linked to a GitHub issue to the Triage project column

on: [issues, pull_request]

jobs:
  add-to-project:
    name: Add issue to project
    runs-on: ubuntu-latest
    steps:
      - uses: actions/add-to-project@v0.4.0
        with:
          project-url: https://github.com/orgs/lambdaisland/projects/2
          github-token: ${{ secrets.GITHUB_TOKEN }}
          
#   github-actions-automate-projects:
#     runs-on: ubuntu-latest
#     steps:  
#     - name: add-new-issues-to-repository-To-project-Triage 
#       uses: docker://takanabe/github-actions-automate-projects:v0.0.1
#       if: github.event_name == 'issues' && github.event.action == 'opened'
#       env:
#         GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#         GITHUB_PROJECT_URL: https://github.com/orgs/lambdaisland/projects/2
#         GITHUB_PROJECT_COLUMN_NAME: Triage
    
#     - name: add-new-prs-to-repository-To-project-Triage
#       uses: docker://takanabe/github-actions-automate-projects:v0.0.1
#       if: github.event_name == 'pull_request' && github.event.action == 'opened'
#       env:
#         GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#         GITHUB_PROJECT_URL: https://github.com/orgs/lambdaisland/projects/2
#         GITHUB_PROJECT_COLUMN_NAME: Triage
        
