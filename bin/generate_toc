#!/usr/bin/env ruby

Dir['doc/*.md'].sort.each do |f|
  title = File.read(f).each_line.first.sub(/^#+/, '').strip
  file  = File.basename(f)
  puts "- [%s](https://cljdoc.xyz/d/lambdaisland/kaocha/CURRENT/doc/%s)" % [title, file]
end
